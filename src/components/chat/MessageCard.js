//import { isOwner } from '../../lib/auth'

function MessageCard({ content }) {
  return (
    <div>
      <div>
        <p>
          {content}
        </p> 
      </div>
    </div>
  )
}

export default MessageCard